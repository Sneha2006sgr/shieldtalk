{
  "name": "shieldtalk",
  "version": "1.0.0",
  "description": "ShieldTalk Defence Communication System",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js",
    "install-all": "npm install --prefix server && npm install --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
